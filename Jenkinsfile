pipeline {
    agent any

    stages {
		stage('build') {
            steps {
				echo "my first pipeline works."
		    		sh 'ls'
		    		sh 'pwd'
		    		echo "second line"
            }
        }
		
	} 
}
